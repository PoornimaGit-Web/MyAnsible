---
- hosts: all
  become: yes
  tasks:
  - name: Install apache2 webserver
    apt:
      name: apache2
      state: present

  - name: starting apache2 websever
    service:
       name: apache2
       state: started